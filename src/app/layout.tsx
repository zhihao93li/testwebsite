import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";
import Header from "@/components/layout/Header";
import Footer from "@/components/layout/Footer";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Vata - Your AI Companion in Spatial Reality",
  description: "Experience the next evolution of human-AI interaction with Vata. Where digital beings become real, and possibilities become infinite.",
  keywords: ["AI companion", "spatial computing", "Vision Pro", "artificial intelligence", "virtual assistant"],
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>
        <Header />
        <main className="min-h-screen">
          {children}
        </main>
        <Footer />
      </body>
    </html>
  );
}
